<<<<<<< HEAD
services:
  - type: web
    name: shl-recommender

    runtime: python

    plan: free

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      python build_index.py

    startCommand: >
      uvicorn main:app
      --host 0.0.0.0
      --port $PORT

    envVars:
      - key: GROQ_API_KEY
        sync: false

      - key: CATALOG_PATH
        value: catalog.json

      - key: CHROMA_DIR
        value: chroma_db

    healthCheckPath: /health

=======
services:
  - type: web
    name: shl-recommender

    runtime: python

    plan: free

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      python build_index.py

    startCommand: >
      uvicorn main:app
      --host 0.0.0.0
      --port $PORT

    envVars:
      - key: GROQ_API_KEY
        sync: false

      - key: CATALOG_PATH
        value: catalog.json

      - key: CHROMA_DIR
        value: chroma_db

    healthCheckPath: /health

>>>>>>> ceb42d9771383095b321b9dff1fb5f19d98878ac
    autoDeploy: true